[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "py-just-to-code-no-framework"
version = "0.1.0"
description = "Standard-library implementation of the just_to_code REST API"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]
